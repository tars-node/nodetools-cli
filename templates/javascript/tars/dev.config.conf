<tars>
	<application>
		<server>
			app=${APP}
			server=${SERVER}
			<${APP}.${SERVER}.${OBJ}Adapter>
				endpoint=tcp -h 127.0.0.1 -p 14001 -t 60000
				servant=${APP}.${SERVER}.${OBJ}
				protocol=tars
			</${APP}.${SERVER}.${OBJ}Adapter>
		</server>
		<client>
		</client>
	</application>
</tars>